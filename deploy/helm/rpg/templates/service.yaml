{{- if gt (int .Values.daemon.healthPort) 0 }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "rpg.fullname" . }}
  labels:
    {{- include "rpg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: health
      protocol: TCP
      name: health
  selector:
    {{- include "rpg.selectorLabels" . | nindent 4 }}
{{- end }}
